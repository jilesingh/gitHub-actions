name: First Workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4
          
        - name: Welcome message
          run: |
            echo "First Github Actions Job"
            ls -ltr
            cat README.md